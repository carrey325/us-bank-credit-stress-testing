# Research-specification changes

The Batch 2 primary specification was frozen on 2026-09-02 before running the
Batch 2 estimator.  Record every subsequent change below; do not overwrite this
entry after observing model results.

| Timestamp | Original specification | New specification | Reason | Changed after seeing results? |
| --- | --- | --- | --- | --- |
| 2026-09-02 | OOS windows beginning 2005 | Expanding OOS windows beginning 2017 (as specified in `configs/model_specs.yaml`) | The real Batch 1 model interface has complete NPL-control coverage beginning in 2017; the prior first two windows have zero complete observations. This was diagnosed before any AR, FE, SPJ, or CRE model result was run. | No |
| 2026-09-02 | `RCON/RCFD1403`-based NPL control and OOS windows beginning 2017 | FDIC BankFind `NCLNLS / LNLSNET` bank-level noncurrent-loan control and original 2005 OOS windows | The real Call Report field available from 2017 is total nonaccrual, not total noncurrent loans. FDIC Financials supplies the correctly defined bank-level numerator and denominator from 2005; the historical coverage and definition were verified before re-estimation. | No |
| 2026-09-04 | CRE interaction used each bank's last CRE-to-Tier1 value on or before 2021Q4 for every 2005–2025 outcome quarter | Primary CRE interaction uses that bank's lagged CRE-to-Tier1 value for each outcome quarter, dated at or before its forecast origin | Reviewer-mandated correction for future-information leakage in the interaction construction. This repair changes timing only; it does not alter approved OOS windows or tune results. | Yes — corrected after reviewer inspection, not for result optimization |
| 2026-09-04 | `COMREPUSQ159N` was treated as a CRE price-index level and subjected to an additional quarter-over-quarter percent-change transformation | Historical `cre_price_growth` is the series' reported year-over-year percent growth; Fed CRE index levels are transformed to matching year-over-year percent growth using four prior quarters of Fed history at the 2026 boundary | Reviewer-mandated Batch 4 data-definition correction. The prior historical feature was a percentage change of an already-signed YoY growth rate, while the stress path was QoQ index growth. This corrects units/definition without changing lags or tuning the specification to results. | Yes — corrected after reviewer inspection, not for result optimization |

## Repair Cycle R2 change register

These entries were recorded before inspecting `VALID_CORRECTED` results. The
`DATA_FIXED_SAME_SPEC` replay had already been observed and is explicitly a
diagnostic replay of inherited implementation defects.

| Timestamp | Classification | Old value | New value | Reason / evidence | Results already observed? | Affected windows |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-07 | DATA_CORRECTION | Pre-R1 credit definitions and silently bridged unsupported filings were present in saved model outputs. | R1 input `mf772-r1-definitions-v2`, including explicit unavailable 031/051 rows and adjacency guards. | R1 validation and official-definition evidence; credit-panel SHA-256 `31d8b08e...61a79`, transition-audit SHA-256 `d1e60dca...ca990`. | Yes; old outputs existed and are retained only as invalid audit comparators. | All four OOS windows; CRE and C&I. |
| 2026-09-07 | IMPLEMENTATION_CORRECTION | `report_date` ambiguously represented both information and target; `forecast_origin = report_date - QuarterEnd()` implied bank filings were known at quarter end. | Persist `report_period`, conservative `available_at = report_period + 45 days`, `forecast_origin = available_at`, and next-quarter `target_period`. | Call Reports are not public automatically at quarter end; R2 requires an explicit conservative availability contract. | Yes; DATA_FIXED_SAME_SPEC replay observed. | All four windows and all forecast rows. |
| 2026-09-07 | IMPLEMENTATION_CORRECTION | Final-vintage fallback macro series were lagged in `macro.py` and then every macro column was shifted again in `modeling.py`. | Apply availability once in the macro layer and carry the origin-known macro value to the target row; retain genuine published/transformation lags. | Code-path inspection and time-alignment audit; the prior path imposed two mechanical quarterly shifts. | Yes; DATA_FIXED_SAME_SPEC replay observed. | All four windows; CRE and C&I. |
| 2026-09-07 | IMPLEMENTATION_CORRECTION | Group `shift(1)` treated the previous observed row as the previous quarter and complete-case filtering required the future outcome before prediction. | Build a quarterly grid first, mask every lag at non-adjacent quarters, and separate `prediction_eligible` from `evaluation_eligible`. | R1 explicitly retains a five-year FFIEC 051 unavailable interval; R2 forbids gap bridging and post-outcome selection. | Yes; DATA_FIXED_SAME_SPEC replay observed. | All windows; especially 2017–2021 FFIEC 051 and the 2025Q4 forecast origin. |
| 2026-09-07 | SPECIFICATION_AMENDMENT | CRE interaction omitted the time-varying exposure main effect. | Include lagged exposure main effect alongside exposure × CRE growth, with bank and quarter FE. | Bank FE do not absorb a time-varying exposure main effect; mandated R2 interpretation correction. | Yes; prior Batch 2 and DATA_FIXED_SAME_SPEC interaction results observed. | Full CRE interaction sample. |
| 2026-09-07 | IMPLEMENTATION_CORRECTION | OOS R² label referred only to `1-SSE/SST`, and FE/AR comparison did not expose weighting target or detailed error contribution. | Name `r2_vs_test_mean` explicitly; add `sse_improvement_vs_ar`, equal-observation and exposure-weighted metrics, and common-key checks. | R2 requires hand-checkable AR-relative metrics and identical scoring keys. | Yes; DATA_FIXED_SAME_SPEC replay observed. | All four windows; CRE and C&I. |
| 2026-09-07 | EXPLORATORY_DIAGNOSTIC | No R2 compact macro-FE diagnostic declaration. | Declare the optional diagnostic disabled before corrected results. | The gate does not need specification search; this prevents post-result activation or automated search. | No VALID_CORRECTED results observed. | None. |
| 2026-09-07 | IMPLEMENTATION_CORRECTION | The frozen Model 2 target-quarter shock was populated from the forecast-safe `lagged_cre_price_growth`, which represents an earlier quarter. | Build a separate final-vintage realized CRE YoY-growth series keyed with `shock_period == target_period`, label it `EX_POST_FINAL_VINTAGE_NOT_FORECAST_ORIGIN_INFORMATION`, and use it only in the associational interaction model. | The frozen formula is Exposure(t-1) × CRE_Price_Shock(t); the availability lag appropriate for prediction cannot replace the target-quarter realized shock in this ex-post hypothesis test. | Yes; corrected after Astra reviewer inspection, without specification or significance search. | Full CRE interaction sample and both authorized robustness variants only. |
| 2026-09-07 | IMPLEMENTATION_CORRECTION | The pre-2022 robustness selected each bank's latest available exposure on or before 2021Q4, allowing a stale pre-gap exposure. | Require the configured exposure date exactly (`2021-12-31`) and explicitly mark/exclude banks without a valid exposure at that date. | A fixed pre-2022 cross-section is not comparable when some banks receive stale values across the known FFIEC 051 reporting gap. | Yes; corrected after Astra reviewer inspection, without specification or significance search. | `pre_2022_exposure_forward` robustness only. |
